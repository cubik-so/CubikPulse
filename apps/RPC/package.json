{
	"name": "@pulse/rpc-proxy",
	"version": "0.0.1",
	"author": "dhruvsol",
	"scripts": {
		"deploy": "wrangler publish src/index.ts",
		"dev": "wrangler dev src/index.ts --local"
	},
	"dependencies": {
		"tsconfig": "workspace:*",
		"wrangler": "^3.10.0"
	}
}
